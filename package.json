{
  "name": "chess-license-netlify",
  "version": "1.0.0",
  "private": true,
  "description": "Netlify deployable paid-license wrapper for a Chinese chess mini game.",
  "scripts": {
    "build": "node build.js",
    "test": "node smoke-test.js"
  },
  "dependencies": {
    "@netlify/blobs": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
